name: Publish @bob-obringer/tsconfig
on:
  push:
    branches:
      - main
    paths:
      - public-packages/tsconfig/**
  workflow_dispatch:
    branches:
      - main
jobs:
  publish:
    uses: ./.github/workflows/_reusable-publish-package.yml
    secrets:
      npm-token: ${{ secrets.NPM_TOKEN_BOB_OBRINGER }}
    with:
      package-path: public-packages/tsconfig
