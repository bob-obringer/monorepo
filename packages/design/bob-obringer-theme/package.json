{
  "name": "@bob-obringer/theme",
  "version": "0.0.1",
  "description": "Theme for Bob Obringer's apps",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:local": "pnpm build",
    "clean": "rimraf dist node_modules",
    "dev": "tsc -w -p tsconfig.dev.json",
    "lint": "eslint --fix 'src/**/*.{ts,tsx}'"
  },
  "dependencies": {
    "@bob-obringer/tw-color-schemes": "workspace:*",
    "@radix-ui/colors": "^3.0.0",
    "tailwindcss": "3.4.3"
  }
}
